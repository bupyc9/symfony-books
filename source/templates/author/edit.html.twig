{% extends 'base.html.twig' %}

{% block title %}{{ 'title.authorEdit'|trans }}{% endblock %}
{% block h1 %}{{ 'title.authorEdit'|trans({'%id%': author.id}) }}{% endblock %}

{% block body %}
    {{ form_errors(form) }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}


